var HTTP = require('../services/HttpService.js');
var Reflux = require('reflux');
var Actions = require('./Actions.jsx');

var React = require('react');

var EmailStore = React.createStore({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = EmailStore;
